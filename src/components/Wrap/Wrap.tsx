import styled from "styled-components";
import { space, layout, flexbox, color } from "styled-system";

const OnWrap = styled.div`
  display: flex;
  flex-wrap: wrap;
  ${space}
  ${layout}
  ${flexbox}
  ${color}
`;

export default OnWrap;
